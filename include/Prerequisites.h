/**
 * @file
 * @brief Archivo principal del sistema. Contiene las librerías estándar utilizadas.
 */

#pragma once

 /// Librerías estándar para entrada y salida
#include <iostream>
/// Librería para manipulación de flujos de texto
#include <sstream>
/// Librería para el uso de vectores dinámicos
#include <vector>
/// Librería para manipulación de cadenas de texto
#include <string>
/// Librería para manipular la salida con formato
#include <iomanip>
/// Librería para funciones relacionadas con caracteres
#include <cctype>
/// Librería con algoritmos estándar como sort, find, etc.
#include <algorithm>
/// Librería para trabajar con representación binaria de datos
#include <bitset>
